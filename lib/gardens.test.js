import test from 'ava';
// import gardens from '..';

test( 'Custom streams work and output correctly', t => {
  // const garden = gardens.createScope();
  // garden.configure({
  //   stream: output => t.is( output, '[log] Hello!\n' )
  // });

  // garden.log( 'Hello!' );

  t.pass();
});
